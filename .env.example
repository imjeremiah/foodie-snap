# FoodieSnap Environment Variables
# Copy this file to .env.local and fill in your actual values

# Supabase Configuration (Required)
EXPO_PUBLIC_SUPABASE_URL=your_supabase_project_url_here
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Private Keys (Set via Supabase secrets, not in .env files)
# SUPABASE_SERVICE_ROLE_KEY=set_via_supabase_secrets  
# OPENAI_API_KEY=set_via_supabase_secrets

# Instructions:
# 1. Copy this file: cp .env.example .env.local
# 2. Replace the placeholder values with your actual Supabase project values
# 3. Set sensitive keys via: supabase secrets set OPENAI_API_KEY=your_key
# 4. Never commit .env.local or any file containing real secrets
