/**
 * @file Chat screen - displays conversations and messaging functionality.
 * Currently a placeholder screen for Phase 1.
 */

import { View, Text } from "react-native";

export default function ChatScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-xl font-semibold text-gray-900">Chat</Text>
      <Text className="mt-2 text-gray-600">Messages and conversations</Text>
    </View>
  );
}
