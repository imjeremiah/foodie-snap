/**
 * @file Blocked Users Route - navigation route for blocked users management.
 * Displays the blocked users management interface.
 */

import BlockedUsersScreen from "../../components/friends/BlockedUsersScreen";

export default function BlockedUsersRoute() {
  return <BlockedUsersScreen />;
} 